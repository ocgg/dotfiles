# Get External IP / Internet Speed
alias myip="curl https://ipinfo.io/json" # or /ip for plain-text ip
alias speedtest="curl -s https://raw.githubusercontent.com/sivel/speedtest-cli/master/speedtest.py | python -"

# Quickly serve the current directory as HTTP
alias serve='ruby -run -e httpd . -p 8000' # Or python -m SimpleHTTPServer :)

# OCGG
alias ls='ls --color=tty --group-directories-first'
alias eza='eza -F --hyperlink --icons --time-style=relative --group-directories-first'
# alias tr='tree -a -I .git' # all files, ignore .git
alias tr='eza -T'
alias trd='tr -D'
alias tra='tr -la' # more file infos
alias tr2="tr -L2" # 2 levels deep
alias tr3="tr -L3" # 3 levels deep

alias notes="nvim -c 'cd ~/notes'"
alias todo="cat ~/notes/todo.md"
alias todoedit="nvim -c 'cd ~/shared/notes' -c 'e todo.md'"
alias plugins="~/code/plugins-ruby/app.rb"
alias xop="xdg-open"
alias nvimconf="nvim -c 'cd ~/.config/nvim' -c 'e init.lua'"
alias conf="nvim -c 'cd ~/code/dotfiles'"
alias nuts="~/code/nuts/nuts"
alias mdr="~/code/md_cli_renderer/mdr"
alias icat="kitten icat"

# For steam os with gamescope
# alias gamemonitor="gamescope --mangoapp -e -- steam -steamdeck -steamos3"
# alias steamos-session-select="steam -shutdown"
